create table java.books(
                           bookId int primary key auto_increment,
                           name varchar(255),
                           description text,
                           publisher varchar(255),
                           author varchar(255)
)